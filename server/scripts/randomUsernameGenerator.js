const adjectives = require("adjectives");
const nouns = require("nouns").nouns;

// console.log(adjectives[Math.floor(Math.random() * adjectives.length)]);
// console.log(nouns[Math.floor(Math.random() * nouns.length)]);
